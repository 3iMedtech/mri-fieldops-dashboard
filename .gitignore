# Local DB dumps — never commit, may contain customer data
*.dump
*.sql.backup
prod-dump-*.sql
staging-dump-*.sql

# macOS
.DS_Store

# Editor / tooling state
.claude/
.vscode/
node_modules/

# graphify knowledge-graph artifacts (re-buildable from source; can leak token costs)
graphify-out/
